// Clarity project id — unique to THIS project; never reuse across apps.
// Replace the placeholder with the real id from clarity.microsoft.com once
// the project is created. Log level is set to None at runtime.
const String kClarityProjectId = 'xmizdjda8l';
